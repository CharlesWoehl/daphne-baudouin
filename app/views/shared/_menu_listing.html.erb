<div class="menu-listing">
  <%= link_to @category.name,root_path, class: "link-logo"   %>
  <div class="sous-menu">
    <ul>
      <% @works.each do |work| %>
        <li>
          <%= link_to work.name, [work.category, work], class: "link-sous-menu" %>
          <div class="year-mini"><%= work.year %></div>
        </li>
      <% end %>
      <li>
  </div>
</div>
